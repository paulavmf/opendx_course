apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: orthanc-postgresql-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/orthanc-postgresql-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/orthanc-postgresql-default: "true"
